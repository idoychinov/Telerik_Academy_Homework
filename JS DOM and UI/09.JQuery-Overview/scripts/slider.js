(function($){
    var Slider = function () {
        var currentIndex = 0;

        function init(selector, interval, elements) {
            var $slider = $(selector);

        }


        return {
            init : init
        }
    }
}(JQuery))